module Gozer (
      ConfigSettings
    , deleteOlder
    , parseConfigFile
    ) where

import           Gozer.Commands (deleteOlder)
import           Gozer.Config   (parseConfigFile, ConfigSettings)
